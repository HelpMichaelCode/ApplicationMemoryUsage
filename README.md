# ApplicationMemoryUsage
PowerShell script that identifies applications using over 500MB of memory at a given moment, highlights the highest consumer, and prompts the user to either terminate the process or leave it running in the background.
